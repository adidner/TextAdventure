import React from 'react';
import { StyleSheet, Text, View } from 'react-native';
import GlobalText from "./GlobalText"

export default function MainMenu(){
  return(
    <View>
      <GlobalText> MainMenu </GlobalText>
    </View>
  );
}
